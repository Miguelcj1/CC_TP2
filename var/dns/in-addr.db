# DNS database file for domain in-addr
# It also includes a pointer to the primary server of the 10. subdomain

@ DEFAULT in-addr.reverse.
TTL DEFAULT 86400

@ SOASP Reverse.in-addr.reverse. TTL
@ SOAADMIN dns\.admin.in-addr.reverse. TTL
@ SOASERIAL 1 TTL
@ SOAREFRESH 14400 TTL
@ SOARETRY 3600 TTL
@ SOAEXPIRE 604800 TTL

# Próprio endereço.
@ NS ns.in-addr.reverse. TTL

# Sub-domain
10.@ NS ns.10.in-addr.reverse. TTL

ns A 10.0.5.14 TTL
ns.10 A 10.0.5.15 TTL